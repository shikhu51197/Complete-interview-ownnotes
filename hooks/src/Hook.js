// it introduced in react version 16.8 and they provide  us ability to hook into core features of react  state management , selecting dom element , component mounting and unmounting etc. they all start with keyword use and they can only we call at the top of our react components  and use with functional component not in class components.


// ex - useState, useEffect, useRef, useContext, useReducer, useCallback, useMemo, useImperativeHandle, useLayoutEffect and useDebugValue.


import React from 'react'

const Hook = () => {
  return (
    <div>
    
    </div>
  )
}

export default Hook
